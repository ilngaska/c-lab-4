#include <iostream>
#include <map>
using namespace std;

int main()
{
    multimap <char, int> m;
    m.insert(pair<char, int>('b', 30));
    m.insert(pair<char, int>('d', 20));
    m.insert(pair<char, int>('e', 40));
    m.insert(pair<char, int>('b', 20));
    m.insert(pair<char, int>('e', 10));
    m.insert(pair<char, int>('c', 10));
    
    for (auto it = m.find('c'); it != m.end(); ++it)
        cout << (*it).first << " " << (*it).second << endl;
        
    cout << m.count('e') << endl;
}
